# sweep-wimbledon-ghost-call
An obscurely named repo for a new challenge by ghost

## Run backend:

- From backend repo, install dependencies `yarn`
- Then run the app `yarn dev`

## Frontend:

- From the base repo
- Serve the index.html file.
